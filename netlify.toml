[build]
  command = "npm run build"
  publish = "dist"
  functions = "functions"
  included_files = ["server/schema.sql"]

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
  force = true
